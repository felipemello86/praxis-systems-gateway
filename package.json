{
  "name": "praxis-systems-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Porta de entrada comercial da suíte (praxis-systems.com.br). Resolve /{cliente} em um hub com os módulos contratados e /{cliente}/{modulo} no módulo real, validando o licenciamento via suite_core antes de encaminhar.",
  "scripts": {
    "dev": "prisma generate && next dev -p 4000",
    "build": "prisma generate && next build",
    "start": "next start -p 4000"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "next": "^14.2.18",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "prisma": "^5.22.0",
    "typescript": "^5.6.3"
  }
}
